/** @type {import('next').NextConfig} */
// Extend when adding image domains, redirects, or env passthrough for this Next app.
const nextConfig = {
  /* config options here */
};

export default nextConfig;
